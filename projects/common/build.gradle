plugins {
    id 'java-library'
}

description = "Common definitions"

dependencies {
    api "hu.webarticum.miniconnect.api:api:${miniConnectApiVersion}"
    api "hu.webarticum.miniconnect:impl:${miniConnectVersion}"
}
